import React from 'react';

import './style.scss';

const Footer = React.createClass({
  render() {
    return (
      <footer className="footer">
        Footer
      </footer>
    );
  }
});

export default Footer;
