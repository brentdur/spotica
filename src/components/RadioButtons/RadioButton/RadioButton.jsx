import React from 'react';

import './style.scss';

const RadioButton = React.createClass({
  render() {
    return (
      <div>
        RadioButton
      </div>
    );
  }
});

export default RadioButton;
